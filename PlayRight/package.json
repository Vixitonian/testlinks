{
  "name": "playright",
  "version": "1.0.0",
  "private": true,
  "description": "Stateless Playwright (Chromium) browser-automation microservice for Render, consumed by a Laravel app.",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "postinstall": "playwright install --with-deps chromium || playwright install chromium"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "playwright": "1.55.1"
  }
}
